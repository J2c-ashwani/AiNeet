# ============================================
# NEET Coach — Environment Variables
# Copy this file to .env.local and fill in values
# ============================================

# ──────────────────────────────────────────────
# 🔴 REQUIRED — App will NOT start without these
# ──────────────────────────────────────────────

# Secret key for signing JWT tokens (generate a random 64-char string)
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# ──────────────────────────────────────────────
# 🟡 RECOMMENDED — Features work in mock mode without these
# ──────────────────────────────────────────────

# Google Gemini API Key (for AI-powered features)
# Get from: https://aistudio.google.com/apikey
# Without this: AI doubts, AI test generation, Vision AI, and NCERT explain return mock responses
GEMINI_API_KEY=

# ──────────────────────────────────────────────
# 🟢 OPTIONAL — Only needed for specific features
# ──────────────────────────────────────────────

# ---------- Database (defaults to SQLite if not set) ----------
# Only set this if you want to use PostgreSQL instead of SQLite
# DATABASE_URL=postgres://user:password@host:5432/neetcoach

# ---------- Supabase (for NCERT PDF storage) ----------
# Only needed if you want NCERT book upload to actually store files
# Get from: https://supabase.com → Project Settings → API
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6...

# ---------- Cashfree Payments ----------
# For accepting subscription payments (Pro ₹199/mo, Premium ₹399/mo)
# Get from: https://merchant.cashfree.com → Developers → API Keys
# Leave empty to use mock payments during development
# CASHFREE_APP_ID=your_app_id
# CASHFREE_SECRET_KEY=your_secret_key

# ---------- Cron Job Security ----------
# Secret to protect the /api/cron/weekly-report endpoint
# Generate a random string and set it in your cron service headers
# CRON_SECRET=your-random-secret-here

# ---------- Email (Resend) ----------
# For sending parent weekly reports and notifications
# Get from: https://resend.com → API Keys
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
# RESEND_FROM_EMAIL=NEET Coach <noreply@yourdomain.com>

# ---------- Sentry Error Tracking ----------
# Only needed if you want error monitoring in production
# Get from: https://sentry.io → Project Settings → DSN
# NEXT_PUBLIC_SENTRY_DSN=https://xxxx@sentry.io/yyyy

# ---------- Node Environment ----------
# Automatically set by hosting platforms, don't set manually
# NODE_ENV=production
