# ===========================================
# AI NEET Coach — Environment Configuration
# ===========================================
# Fill in your credentials below and rename this file to .env.local
# (Next.js auto-loads .env.local and keeps it out of git)

# ------------------------------------------
# 🗄️ DATABASE (Supabase Postgres)
# ------------------------------------------
# Go to: Supabase Dashboard → Settings → Database → Connection String (URI)
# Use the "Transaction" connection string for serverless
DATABASE_URL=postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# ------------------------------------------
# 🤖 AI — Google Gemini API Key
# ------------------------------------------
# Go to: https://aistudio.google.com/apikey
# Create a new API key and paste it here
GEMINI_API_KEY=your_gemini_api_key_here

# ------------------------------------------
# 🔐 AUTH — JWT Secret
# ------------------------------------------
# Any long random string (used to sign auth tokens)
# Generate one: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_change_in_production

# ------------------------------------------
# 💰 PAYMENTS — Razorpay (Optional)
# ------------------------------------------
# Go to: https://dashboard.razorpay.com → Settings → API Keys
# Leave as mock values if not using real payments yet
RAZORPAY_KEY_ID=rzp_test_your_key_here
RAZORPAY_KEY_SECRET=your_razorpay_secret_here

# ------------------------------------------
# ⏰ CRON — Weekly Report Secret (Optional)
# ------------------------------------------
# Used to authenticate the /api/cron/weekly-report endpoint
# Any random string — must match what your cron service sends
CRON_SECRET=your_cron_secret_here

# ------------------------------------------
# 📊 MONITORING — Sentry (Optional)
# ------------------------------------------
# Go to: https://sentry.io → Project → Settings → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://your_key@sentry.io/your_project_id

# ------------------------------------------
# 🌐 APP CONFIG
# ------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
